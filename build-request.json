{
  "kind": "build_request",
  "title": "Reject Firebase integration — unsupported platform",
  "priority": "normal",
  "requirements": [],
  "constraints": [
    "Firebase and all external cloud databases (AWS, GCP, Azure, etc.) are not supported on this platform.",
    "All data persistence must use the ICP backend (Motoko actor) or localStorage — no third-party database services."
  ],
  "nonGoals": [
    "Firebase Firestore, Firebase Realtime Database, or any Firebase SDK integration",
    "Any external cloud database integration"
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}